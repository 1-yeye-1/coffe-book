# 测试报告

## 测试环境

| 项目 | 说明 |
| --- | --- |
| 操作系统 | Windows 本地开发环境 |
| Node.js | 18+，已在 Node.js 22 环境验证 |
| 数据库 | MySQL 8 或兼容版本 |
| 前端 | Vue 3 + Vite |
| 后端 | Node.js 原生 HTTP 服务 |
| 浏览器测试 | Playwright |

## 测试范围

- 用户注册、密码登录、短信验证码登录。
- 未登录访问会员接口应失败。
- 普通用户不能访问后台接口。
- 用户只能查看自己的订单和预约。
- 管理员登录。
- 后台商品新增、修改、查询、删除。
- 后台订单支付状态修改。
- 商品浏览、加入购物车、创建订单、支付流程。
- 座位预约和我的预约。
- 社区发帖、评论、点赞、后台审核。
- 活动创建和报名。
- E2E 临时数据自动清理。

## 测试用例表

| 编号 | 用例 | 类型 | 预期结果 |
| --- | --- | --- | --- |
| T01 | 管理员登录 | 接口测试 | 返回管理员 token |
| T02 | 未带 token 访问后台汇总 | 接口测试 | 返回 401 |
| T03 | 后台新增并修改商品 | 接口测试 | 商品写入并更新成功 |
| T04 | 用户注册 | 接口测试 | 返回用户 token |
| T05 | 用户密码登录 | 接口测试 | 登录成功 |
| T06 | 短信验证码登录 | 接口测试 | 登录成功，验证码不返回前端 |
| T07 | 未登录访问 `/api/member` | 接口测试 | 返回 401 |
| T08 | 普通用户访问后台接口 | 接口测试 | 返回 401 |
| T09 | 用户查询其他人的订单支付状态 | 接口测试 | 返回 403 |
| T10 | 用户查看会员中心预约列表 | 接口测试 | 不包含其他用户预约 |
| T11 | 用户取消其他人的预约 | 接口测试 | 返回 403 |
| T12 | 后台审核订单支付 | 接口测试 | 订单状态更新 |
| T13 | 首页访问 | E2E | 页面首屏可见 |
| T14 | 未登录访问会员中心 | E2E | 跳转登录页 |
| T15 | 登录表单非法输入 | E2E | 展示校验提示 |
| T16 | 后台删除商品 | E2E | 出现二次确认弹窗 |
| T17 | 用户下单流程 | E2E | 进入支付页面 |
| T18 | 用户预约流程 | E2E | 进入我的预约页面 |
| T19 | 社区发帖流程 | E2E | 帖子出现在列表 |
| T20 | E2E 数据清理 | E2E | 临时商品、订单、预约、帖子无残留 |

## 测试结果

执行日期：2026-06-02

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `npm run lint` | 通过 | `lint ok (32 checks)`，检查 package、脚本语法、冲突标记和新增后端模块 |
| `npm run build` | 通过 | Vite 前端生产构建成功 |
| `npm run test` | 通过 | `backend api smoke test ok`，后端接口冒烟测试通过 |
| `npm run test:e2e` | 通过 | `10 passed`，Playwright 关键浏览器流程通过 |

## 问题与修复

| 问题 | 修复 |
| --- | --- |
| 根目录一键启动在 Windows + Node 22 下调用 `npm.cmd` 可能 `spawn EINVAL` | 改为直接用 Node 启动前端脚本 |
| E2E 外层 cleanup 偶发 `fetch failed` | 将本轮数据清理放入 Playwright 测试内部，并断言清理结果 |
| 后端大文件中重复校验、会员和社区逻辑较多 | 抽出 `validators`、`audit`、`modules/member`、`modules/community`、`modules/admin-summary` |
| MySQL 常用查询缺少显式索引 | 增加启动时索引检查与创建 |
| 500 错误可能暴露底层错误信息 | 未捕获异常只在后端终端记录，前端返回友好提示 |

## 总结

当前项目已经具备课程设计/实验项目提交所需的基本验收能力：前后端可启动，数据库可自动初始化，核心业务流程可运行，权限和资源归属有自动化测试覆盖，文档能够支撑答辩讲解和本地部署。
